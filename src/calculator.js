const sum = (a, b) => {
  return a + b;
}

const subtraction = (a, b) => {
  return a - b;
}

const division = (a, b) => {
  return a / b;
}

const multiplication = (a, b) => {
  return a * b;
}

module.exports = {
  sum,
  subtraction,
  division,
  multiplication,
}
